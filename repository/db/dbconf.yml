test:
    driver: $DB_DRIVER
    open: $TEST_DRONE_DATABASE

development:
    driver: $DB_DRIVER
    open: $DEV_DRONE_DATABASE

environment_variable_config:
    driver: $DB_DRIVER
    open: $DEV_DRONE_DATABASE